# Mobile (React Native + Expo)

## Run
1. Copy `.env.example` to `.env`
2. Install dependencies: `npm install`
3. Start: `npm run start`

This app includes starter screens for student discovery, owner dashboard, and profile.
